From the RPG conversation below, extract the player character's current name and stats. Reply with ONLY a single JSON object, no markdown, no code block, no explanation. Use exactly these keys where known: {{field_list}}. Use character_name for the character's name. Omit any key whose value is not stated in the conversation. Numbers must be numeric (e.g. 14 not "14").

Conversation:
{{conversation}}